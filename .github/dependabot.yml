# Keep the SHA-pinned GitHub Actions up to date automatically (recommended by codex review).
version: 2
updates:
  - package-ecosystem: github-actions
    directory: "/"
    schedule:
      interval: weekly
