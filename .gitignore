# Rust
/target
**/*.rs.bk

# Agent-local config (contains machine-specific absolute paths; not shared).
# Hook config generated by agmsg when a delivery mode is set.
.codex/hooks.json
.claude/settings.local.json
