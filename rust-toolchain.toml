# Pin the toolchain for reproducibility across implementations (implementation plan P0).
# Latest-stable policy (CLAUDE.md). When bumping, keep this in sync with Cargo.toml's rust-version.
[toolchain]
channel = "1.97.0"
components = ["rustfmt", "clippy"]
